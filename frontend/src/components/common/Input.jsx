// import React from "react";

// export function Input({ label, type = "text", value, onChange, placeholder, required, disabled, error, className = "", full = false, ...props }) {
//   return (
//     <div className={`form-group ${full ? "full" : ""} ${className}`}>
//       {label && <label className="form-label">{label} {required && "*"}</label>}
//       <input
//         type={type}
//         className="form-input"
//         value={value}
//         onChange={onChange}
//         placeholder={placeholder}
//         disabled={disabled}
//         {...props}
//       />
//       {error && <span style={{ color: "#dc2626", fontSize: 12, marginTop: 4 }}>{error}</span>}
//     </div>
//   );
// }

// export default Input;
